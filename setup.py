from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='The Beer Type Prediction project uses a custom neural network to predict the type of beer based on a user's rating criteria',
    author='Sean Williams',
    license='',
)
